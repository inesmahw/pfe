{% extends 'base.html.twig' %}

{% block title %}Edit Astuces{% endblock %}

{% block body %}
    <h1>Edit Astuces</h1>

    {{ include('astuces/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('astuces_index') }}">back to list</a>

    {{ include('astuces/_delete_form.html.twig') }}
{% endblock %}
